import { createStore } from 'vuex'

import popupMenu from './popupMenu'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    popupMenu
  }
})
